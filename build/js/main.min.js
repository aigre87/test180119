function init(){camera=new THREE.PerspectiveCamera(45,window.innerWidth/window.innerHeight,.1,1e3),camera.position.z=5,camera.position.y=5,camera.position.x=5,scene=new THREE.Scene,geometry=new THREE.BoxGeometry(1,1,1),material=new THREE.MeshNormalMaterial;const e=new THREE.GridHelper(20,20);scene.add(e),mesh=new THREE.Mesh(geometry,material),scene.add(mesh),renderer=new THREE.WebGLRenderer({antialias:!0,alpha:!0}),renderer.setSize(canvasWrapper.clientWidth,canvasWrapper.clientHeight),canvasWrapper.appendChild(renderer.domElement);new THREE.OrbitControls(camera,renderer.domElement)}function onWindowResize(){camera.aspect=canvasWrapper.clientWidth/canvasWrapper.clientHeight,camera.updateProjectionMatrix(),renderer.setSize(canvasWrapper.clientWidth,canvasWrapper.clientHeight)}function animate(){requestAnimationFrame(animate),mesh.rotation.x+=.01,mesh.rotation.y+=.02,renderer.render(scene,camera)}var camera,scene,renderer,geometry,material,mesh,canvasWrapper=document.getElementById("canvasWrapper");init(),animate(),window.addEventListener("resize",onWindowResize,!1);